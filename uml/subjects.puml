@startuml Subjects

class Subjects {
    .. Context Usage ..
    + useTimetable() hook
    + useSettings() hook
    __
    - JSX.Element render()
}

class Subject {
    .. props ..
    + Subject subject
    .. Context Usage ..
    + useTimetable() hook
    + useSettings() hook
    .. state ..
    + boolean isOpen
    + boolean isRefreshing
    __
    + async updateButton()
    + void editButton(subject)
    + void handleChange()
    + string getStatus()
    + string getIcon()
    + JSX.Element render()
}

class SubjectModal {
    .. props ..
    + Subject subject
    .. Context Usage ..
    + useTimetable() hook
    __
    + void handleSave(event)
    + JSX.Element render()
}

class SubjectTimetable {
    .. props ..
    + Subject subject
    .. Context Usage ..
    + useSettings() hook
    __
    + JSX.Element render()
}

class CourseAdder {
    .. props ..
    + number subjectId
    .. Context Usage ..
    + useTimetable() hook
    __
    + void handleSubmit(event)
    + JSX.Element render()
}

class CourseTable {
    .. props ..
    + Subject subject
    .. Context Usage ..
    + useTimetable() hook
    + useSettings() hook
    __
    + JSX.Element render()
}

class CourseModal {
    .. props ..
    + Subject subject
    + string courseId
    .. Context Usage ..
    + useTimetable() hook
    + useSettings() hook
    __
    + void handleSave(event)
    + JSX.Element render()
}

Subjects o-- "many" Subject
Subject o-- SubjectModal
Subject o-- SubjectTimetable
Subject o-- CourseAdder
Subject o-- CourseTable
CourseTable o-- "many" CourseModal

note right of Subject
    Modern funkcionális komponens
    hook-okkal és Context API-val
    Frissítési funkcionalitás hozzáadva
end note

note right of SubjectTimetable
    Alternatív órarend nézet
    egyedi tárgyakhoz
end note

@enduml